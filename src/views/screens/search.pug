extends ../base
include ../mixins/video


block contents
    form(method="get").search__form
        input(name="keyword", placeholder="Search Title", type="text", required)
        button
            i.fas.fa-search
    div.video-grid
        each video in videos 
            +videoMixin(video)
        else 
            span.empty__message=message
        